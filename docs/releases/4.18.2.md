# Core 4.18.2 runtime repair

Status: release preparation, not proof of publication.

This patch corrects byte-split UTF-8 decoding and successful-terminal validation in the shared Codex JSONL execution path. Streaming and non-streaming parsing now use the same state machine. An earlier agent message cannot authorize success after terminal failure or without `turn.completed`. Invalid/truncated UTF-8 fails closed instead of silently changing model output.

The release keeps existing Safe Contract, Policy, Receipt and Provider schema versions. The shipped runtime bytes change, so runtime-digest equivalence to Core 4.18.1 must not be claimed. Supported Node/OS tests, package reproducibility, required review and the trusted release workflow remain mandatory.

After the exact immutable release exists, upgrade the five stable consumers through the coordinated Family transaction, verify their required product distributions/CI receipts, and publish a fresh Family manifest. Do not pin consumers to this pull-request head. Debug remains development.

The separate Family/Canary governance repair must also be integrated and its real scheduled/manual qualification executed. A green credentialless pull-request canary does not prove live behavior or quality.
